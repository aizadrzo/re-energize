{
  "name": "re-energize",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "css": "node-sass --include-path scss ./src/scss/style.scss ./src/styles/style.css",
    "watch": "nodemon -e scss -x 'npm run css'"
  },
  "author": "Aizad Ridzo",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^9.0.0",
    "nodemon": "^3.0.1"
  }
}
